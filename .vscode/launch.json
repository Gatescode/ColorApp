{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "expo start --tunnel",
            "name": "Run npm start",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}